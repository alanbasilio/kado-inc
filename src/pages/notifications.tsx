import type { NextPage } from "next";

import Layout from "@/components/dashboard-layout";

const Notifications: NextPage = () => {
  return <Layout title="Notifications"></Layout>;
};

export default Notifications;
