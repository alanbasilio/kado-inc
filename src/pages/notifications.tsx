import type { NextPage } from "next";

import Layout from "@/components/dashboard-layout";

const Notifications: NextPage = () => {
  return <Layout title="Notifications">IN PROGRESS</Layout>;
};

export default Notifications;
