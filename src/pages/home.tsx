import type { NextPage } from "next";

import Layout from "../components/dashboard-layout";

const Home: NextPage = () => {
	return (
		<Layout>
			<h1>Dashboard</h1>
		</Layout>
	);
};

export default Home;
