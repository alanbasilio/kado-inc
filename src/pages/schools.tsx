import type { NextPage } from "next";

import Layout from "@/components/dashboard-layout";

const Schools: NextPage = () => {
  return <Layout title="Schools">IN PROGRESS</Layout>;
};

export default Schools;
