import type { NextPage } from "next";

import Layout from "@/components/dashboard-layout";

const Departments: NextPage = () => {
  return <Layout title="Departments">IN PROGRESS</Layout>;
};

export default Departments;
