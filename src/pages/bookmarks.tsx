import type { NextPage } from "next";

import Layout from "../components/dashboard-layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <h2>Bookmarks</h2>
    </Layout>
  );
};

export default Home;
