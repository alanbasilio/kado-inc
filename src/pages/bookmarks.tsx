import type { NextPage } from "next";

import Layout from "@/components/dashboard-layout";

const Bookmarks: NextPage = () => {
  return <Layout title="Bookmarks">IN PROGRESS</Layout>;
};

export default Bookmarks;
