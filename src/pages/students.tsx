import type { NextPage } from "next";

import Layout from "@/components/dashboard-layout";

const Students: NextPage = () => {
  return <Layout title="Students">IN PROGRESS</Layout>;
};

export default Students;
